# Art Institute of Chicago – Artworks Table

A React + TypeScript application that displays artworks from the Art Institute of Chicago API using **server-side pagination** and **persistent row selection**.

Built as part of a React Internship Assignment.

---

## 🚀 Features

- Server-side pagination using PrimeReact DataTable
- Fetches data page-by-page from API (no prefetching)
- Persistent row selection across pages
- Checkbox-based row selection
- Custom row selection overlay (select N rows from current page)
- Clean, professional UI with pagination report
- Handles missing values gracefully (`N/A`)

---

## 🛠 Tech Stack

- React
- TypeScript
- Vite
- PrimeReact
- PrimeIcons

---

## ⚙️ Project Setup (Local)

### 1️⃣ Clone the repository

```bash


git clone https://github.com/shalinikatore32/artwork-table.git
cd artwork-table
```

### 2️⃣ Install dependencies

```bash
npm install
```

### 3️⃣ Start development server

```bash
npm run dev

```

# The app will run at:

```bash
http://localhost:5173

```
